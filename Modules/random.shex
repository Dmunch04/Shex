-- Import with: imp ('random')

-- Lowercased functions
task randint (x, y)
  var res = `import random;;random.randint (int (Local['x'].Value), int (Local['y'].Value))`
done

-- Uppercased (First letter) functions
task RandInt (x, y)
  var res = `import random;;random.randint (int (Local['x'].Value), int (Local['y'].Value))`
done
