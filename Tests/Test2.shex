-- STATUS: SOLVED


task random (x, y)
  var res = `import random;;random.randint (int (Local['x'].Value), int (Local['y'].Value))`
  var x = 'a'

  res -- this is like returning. You just write the vars name, or the value you wanna return at the end of the function
done

var x = random (1, 10)
say (x)

var print = say
var yeet = print
yeet ('ya yeet')
